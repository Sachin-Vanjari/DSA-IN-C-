void bubbleSort(vector<int>& arr, int n)
{   
    int j = 1;
    while(j < n)
    {
        for(int i = 0; i < n-j; i++)
        {
            if(arr[i] > arr[i+1])
            swap(arr[i],arr[i+1]);
        }
        j++;

    }
}
